version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step"
    build:
      commands:
        - echo "No build step"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
